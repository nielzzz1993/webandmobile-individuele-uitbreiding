<nav class="navbar navbar-expand-lg">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
    <span class="navbar-toggler-icon">&#9776;</span>
  </button>

  <!-- Brand -->
  <a class="navbar-brand" href="{{ path('messages')}}">
    <img src="{{ asset('assets/images/logoPXL.png') }}" width="45pt" padding="2pt">
  </a>

  <!-- Links -->
  <div class="collapse navbar-collapse" id="collapsibleNavbar">
    <ul class="nav navbar-nav">
        <a class="nav-link" href="{{ path('messages')}}">Home</a>
      </li>
      {% if not is_granted('ROLE_USER') %}
        <li class="nav-item">
          <a class="nav-link" href="/login">Login</a>
        </li>
      {% endif %}
      {% if is_granted('ROLE_USER') %}
        <li class="nav-item">
          <a class="nav-item nav-link" href="{{ path('app_logout')}}">Logout</a>
        </li>
        <li class="nav-item">
          <a class="nav-item nav-link" href="{{ path('profile')}}">Profile</a>
        </li>
      {% endif %}
      {% if is_granted('ROLE_POSTER') %}
        <li class="nav-item">
          <a class="nav-item nav-link" href="{{ path('message-add')}}">Create Message</a>
        </li>
      {% endif %}
      {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-item nav-link" href="{{ path('user.create')}}">Create User</a>
        </li>
        <li class="nav-item">
          <a class="nav-item nav-link" href="{{ path('user.dashboard')}}">User Dashboard</a>
        </li>
      {% endif %}
    </ul>
    <form class="form-inline ml-auto" action="/action_page.php" onsubmit="return process();">
      <input class="form-control mr-sm-2" type="text" placeholder="Search messages" name="search" id="search">
      <button class="btn btn-success" type="submit">Search</button>
    </form>
    <!--form class="card card-sm" onsubmit="return process();"> <div class="card-body row no-gutters align-items-center"> <div class="col-auto"> <i class="fas fa-search text-body"></i> </div> <div class="col"> <input class="form-control
    form-control-borderless" name="search" id="search" type="search" placeholder="Search messages"> </div> <div class="col-auto"> <button class="btn btn-success" type="submit">Search</button> </div> </div> </form--->
  </div>
</nav>